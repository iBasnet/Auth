
export type ToDoT = {
    dueBy: Date,
    task: string,
    isComplete: Boolean,
    category: string
    _id?: string;
    createdAt?: string;
    updatedAt?: string;
}

export type UserDateT = {
    joined: string;
    logged: string;
}